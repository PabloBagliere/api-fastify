name: Node.js CI

on: [push]

jobs:
  ci-test:
    runs-on: ubuntu-latest
    staps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: npm ci
      - run: npm test